import { FormatClpPipe } from './format-clp.pipe';

describe('FormatClpPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatClpPipe();
    expect(pipe).toBeTruthy();
  });
});
